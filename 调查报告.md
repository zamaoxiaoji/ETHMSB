# 调查报告：基于 PBS 的多位整数 MSB 提取失败机理与 Δ/2 Offset 通用门实验

## 1. 问题（要解决什么）

在 HE3DB/TFHEpp 体系中，我们希望对**多位整数密文**（明文占用 `plain_bits=t` 位）提取其最高位（MSB）。现象是：

- HE3DB 中的 `ExtractMSB5 / MSBGateBootstrapping` 这类“单次 PBS 门”实现，**在输入位宽超过其设计范围时会出现错误**。
- 我们提出一个自然改法：对任意 `t`，将门内的 `offset` 改为步长的一半 `Δ/2`，并尝试复用同一个 LUT（同一类 μ-多项式 testvector）来做 MSB 判定；但随着 `t` 增大仍可能出现失败。

本实验要回答的核心是：失败究竟来自哪里？是普通“跨 half 阈值”，还是“量化/模数切换导致的回绕（carry/wrap-around）”？

---

## 2. 背景（为什么会有这个问题）

### 2.1 TLWE 中整数编码与 phase

设 torus 位宽为 `W`（lvl1 常见为 32），明文位宽为 `t=plain_bits`。常见的多值整数编码方式是将整数 `m` 放到 torus 的高 `t` 位：

- 步长：`Δ = 2^(W - t)`
- 理想相位（ideal）：`ideal = (m << (W - t)) mod 2^W`

TLWE 的“相位”（debug 用）可以由密钥直接计算：

> `phase = b - Σ(a_i * s_i) (mod 2^W)`

其中 `a` 是密文前 `k*n` 个系数，`b` 是最后一项；`s` 为 secret key。

### 2.2 MSB 门为何要加 offset

PBS（programmable bootstrapping）本质是在一个“旋转指数域”里根据输入相位选取/旋转 testvector（LUT），从而实现阈值判定。

在 HE3DB 的 `MSBGateBootstrapping` 里，存在固定的 `offset`（相当于对相位做半步偏移），其设计动机是把判定阈值对齐到量化格点，减少边界歧义。但当输入的 `plain_bits` 改变时，固定 offset 可能落入 message 区，改变有效明文并导致 LUT 语义失配。

### 2.3 “通用门”想法：offset=Δ/2

自然的修正思路：对任意 `t`，用步长的一半作为偏移：

> `offset = Δ/2 = 2^(W - t - 1)`

直觉是：这能让 MSB 判定在“每个 message cell 的中心”附近进行，从而减少误判。

但随着 `t` 增大，`Δ` 变小，任何量化/舍入引入的等效误差都更容易“侵入 message 区”，于是即便 offset 做了自适应，仍可能出现失败。

### 2.4 两类 worst-case（msb0 / msb1）

为了区分不同失败机制，本实验使用两类典型输入：

- `msb0`：`m = 2^(t-1) - 1`（形如 `0111..11`，期望 MSB=0）  
  特点：理想相位靠近 `2^(W-1)`（half）阈值下方，适合观察“跨 half”型错误。
- `msb1`：`m = 2^t - 1`（形如 `1111..11`，期望 MSB=1）  
  特点：理想相位非常接近 `2^W` 回绕点，适合观察“跨 2^W 回绕 / carry chain”型错误。

---

## 3. 实验目标与假设（要验证什么）

### 3.1 目标

1) 在 `offset=Δ/2` 的通用 MSB 门下，扫描不同 `plain_bits`，统计是否出现失败。  
2) 对于失败样本，输出可复现的 trace，用证据链定位失败来自以下哪类机制：

- **机制 A：跨 half 阈值**（典型对应 `msb0`）。  
  量化误差导致“量化相位 index”从 half 下方变到 half 上方。
- **机制 B：回绕/进位（wrap-around/carry）**（典型对应 `msb1`）。  
  输入相位本来接近 `2^W`，量化误差使 index 发生模 `2N` 回绕：从接近最大值跳到接近 0，从而导致 MSB 翻转（1→0）。

### 3.2 关键证据链（不看内部实现也能证明）

由于不允许修改 HE3DB/TFHEpp 源码，本实验通过“复制关键函数到新命名空间 + 插桩”的方式，在失败样本复跑时打印：

- `phase`（输入 TLWE）与 “加 offset 后 TLWE” 的相位布局（`m区 | Δ间隔区 | 误差区`）
- IKS（IdentityKeySwitch）后 lvl0 TLWE 的相位（这是进入 BlindRotate 之前的输入）
- BlindRotate 内部的两种“量化相位”对照：
  - `q_phase_direct`：由精确 `phase` 直接右移量化得到
  - `q_phase_parts`：由 `qb - Σ(ai_q*s_i)` 组合得到（BlindRotate 实际路径）
  若两者在 msb（>=half）判断上分歧，且出现 `q_phase_parts` 回绕到接近 0，则支持“量化层回绕/进位”假设。

---

## 4. 实验方案（怎么做）

### 4.1 约束（必须遵守）

- 不修改 HE3DB 既有源码：`src/`、`include/`、`test/`、`thirdparty/` 下已有文件均不改。
- 插桩仅通过“复制函数实现到 experiments/ 下的新文件、新命名空间”完成；实验程序显式调用复制版接口。
- 不在大循环内无条件打印；仅在检测到失败样本后用同一 seed 复跑并开启 trace，且每个 `plain_bits` 最多打印 `K` 条。

### 4.2 实现思路（分层）

1) **phase 计算与三段格式打印**（只用于 debug/证据链）  
   计算 `phase = b - Σ(a_i*s_i) (mod 2^W)`，并按
   ```
   <m区(t位)> | <Δ间隔区(W-t-err_bits位)> | <误差区(err_bits位)>
   ```
   固定宽度输出，便于肉眼比较“message 是否被侵入 / 是否发生回绕”。

2) **复制 TFHEpp 的 BlindRotate 与 GateBootstrappingTLWE2TLWEFFT**  
   放入 `TFHEppInstrumented` 命名空间，保持行为一致，仅增加可控 trace，输出 `qb/bbar/ai_q` 与 `q_phase_direct/q_phase_parts` 对照。

3) **复制/实现通用 MSB 门（Δ/2 offset）**  
   在 experiments 内实现一条 PBS 门链路：
   - 输入 TLWE（lvl1 或 lvl2）
   - 加 `offset=2^(W-t-1)`
   - IKS 到 lvl0（进入 BlindRotate 前）
   - 调用复制版 `GateBootstrappingTLWE2TLWEFFTTrace`（从而触发 BlindRotateTrace）
   - SampleExtract 得到输出 bit TLWE
   - 解密输出 bit 并与期望比较

4) **实验程序扫描 + 失败复跑**  
   对每个 `plain_bits` 重复 `trials` 次：
   - 每次 trial 用确定性 seed 重置随机生成器，保证失败样本可复现
   - 计算 `expected_msb`（由明文直接得到）
   - 运行通用门得到 `got`（解密 bit）
   - 若失败：记录 seed，并立即用同 seed 复跑一次，打开 trace 打印证据链

---

## 5. 实验细节（实现到代码层面的约定）

### 5.1 关键参数与符号

- `W`：torus 位宽（lvl1 常见 32；lvl2 常见 64）
- `t=plain_bits`：明文位数
- `Δ = 2^(W-t)`
- `offset = Δ/2 = 2^(W-t-1)`
- `phase`：`b - Σ(a_i*s_i) (mod 2^W)`（仅用于 debug）

### 5.2 BlindRotate 的量化与 shift_bs

BlindRotate 在进行 modulus switching 时会把 torus 相位量化到旋转指数域，其量化右移位数为：

> `shift_bs = digits(domainT) - 1 - targetP::nbit + bitwidth`

在默认 lvl01（domain=lvl0, target=lvl1, nbit=10, 单输出 bitwidth=0）下，`shift_bs` 固定。它由参数集决定，**不是随输入 `plain_bits` 动态变化的业务参数**。

实验中我们通过 trace 输出 `shift_bs`，用来解释：当 `t` 增大时，`Δ/2` 换算到量化域的“余量 bin 数”会快速变小，从而更容易被量化误差推过阈值（half 或 mod 回绕点）。

### 5.3 复现性（seed）

实验程序在每个 trial 开始前设置：

> `TFHEpp::generator.seed(seed)`

并在失败样本上用同一 seed 复跑，保证：

- 重新加密得到同分布的噪声样本
- 进入 PBS 的路径一致
- trace 输出可复现

### 5.4 打印策略（避免刷屏）

- 默认 trial 循环不打印。
- 仅当 `got != expected` 时打印，并且每个 `plain_bits` 最多打印 `K` 条。
- BlindRotateTrace 内部不打印全量 `a_i`，仅打印前 `max_nonzero_a_print` 个非零 `ai_q` 样本。

### 5.5 对照输入（control）

每个 `plain_bits` 可选对照输入：

> `m3 = 3 * 2^(t-2)`（形如 `11..00`）

其目的是验证：失败是否主要集中在 worst-case（`msb0/msb1`）这种“贴边”输入上，而非普遍错误。

---

## 6. 文件清单（实验新增/使用）

### 6.1 新增文件（experiments）

- `experiments/he3db_phase_trace.hpp`  
  phase 计算与三段格式打印工具。
- `experiments/tfhepp_instrumented_pbs.hpp`  
  复制 TFHEpp 的 `BlindRotate` 与 `GateBootstrappingTLWE2TLWEFFT`，添加可控 trace，输出 `q_phase_direct/q_phase_parts` 等关键证据。
- `experiments/he3db_msb6.hpp`  
  “通用 MSB 门（offset=Δ/2）”实现（新命名空间），并在 trace 模式输出输入/offset/IKS/lvl0 以及 BlindRotateTrace 与输出 bit 解密。
- `experiments/msb_delta_half_fail_trace.cpp`  
  主实验程序：扫描 `plain_bits`、`msb0/msb1` 两类 worst-case，失败复跑打印 trace。

（本仓库同时包含更早期用于对照/机制验证的实验，如 `experiments/msb5_fail_trace.cpp` 等，但本报告聚焦于 Δ/2 通用门实验。）

### 6.2 只读依赖（不修改）

- HE3DB：`src/HEDB/comparison/tfhepp_utils.cpp/.h`、`src/HEDB/comparison/extract_msb.cpp` 等
- TFHEpp：`thirdparty/TFHEpp/include/gatebootstrapping.hpp`、`thirdparty/TFHEpp/include/utils.hpp` 等

---

## 7. 构建与运行（不含结果）

### 7.1 构建

在仓库根目录：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j --target msb_delta_half_fail_trace
```

生成的可执行文件：`build/bin/msb_delta_half_fail_trace`

### 7.2 运行参数

```bash
./build/bin/msb_delta_half_fail_trace --help
```

常用参数：

- `--level 1|2`：输入密文等级（1=TLWELvl1，2=TLWELvl2）
- `--min/--max`：扫描 plain_bits 范围
- `--trials`：每个 plain_bits 的试验次数
- `--case msb0|msb1`：选择 worst-case 类型
- `--K`：每个 plain_bits 最多打印 K 个失败样本 trace
- `--err-bits`：三段格式中“误差区”的低位 bit 数
- `--max-nonzero-a`：BlindRotateTrace 内最多打印多少个非零 `ai_q`
- `--base-seed`：随机种子基数
- `--stop`：出现失败后停止继续扫描

---

## 8. 如何阅读 trace（不含实验结果）

当出现失败样本并触发 trace 时，建议按如下顺序解读：

1) **输入 phase 与 ideal 的关系**  
   - `phase(三段)` 中的 `m区` 是否与输入 `m` 一致  
   - `error_mod / e_centered` 是否主要集中在低位误差区

2) **加 offset 后是否已“贴边”**  
   - `msb0`：关注是否接近 half（`2^(W-1)`）  
   - `msb1`：关注是否接近回绕点（`2^W`）

3) **进入 BlindRotate 前（IKS 后 lvl0）的 phase**  
   这是 BlindRotate 实际接收的输入（domainP），可以判断误差在 KeySwitch 阶段是否放大/偏移。

4) **BlindRotateTrace：q_phase_direct vs q_phase_parts**  
   - `q_phase_direct`：由精确 phase 量化得到的基准
   - `q_phase_parts`：由 BlindRotate 的 `qb - Σ(ai_q*s_i)` 得到的实际量化相位
   - 若二者的 `msb(>=half)` 判断不同：
     - `msb0` 更可能表现为“跨 half”
     - `msb1` 若 `q_phase_parts` 变得很小/接近 0，则更符合“回绕/carry”解释

5) **输出 bit 解密值**  
   输出 TLWE 的 phase 已不再表示原整数 `m`，因此只以“解密 bit=0/1”作为最终判定。

---

## 9. 结论形式（本报告不含结果）

本实验通过：

- 可复现随机性（seed 控制）
- 仅失败样本复跑的插桩策略
- TFHEpp BlindRotate 内部“量化相位两条路径”的对照（direct vs parts）

给出一条可操作的证据链，用于在不修改库代码的前提下判断 MSB 失败属于：

- 半区阈值翻转（msb0 型），还是
- 模数回绕/进位（msb1 型）。

（具体统计结果、失败率与示例 trace 输出不包含在本报告中。）

